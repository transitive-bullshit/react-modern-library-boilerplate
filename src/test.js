import { Foo, Bar } from './'

describe('Foo', () => {
  it('is truthy', () => {
    expect(Foo).toBeTruthy()
  })
})

describe('Bar', () => {
  it('is truthy', () => {
    expect(Bar).toBeTruthy()
  })
})
